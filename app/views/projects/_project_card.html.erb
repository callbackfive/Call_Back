
<%= image_tag @project.image_url if @project.image_url.present? %>
<div>專案類別： <%= @project.category %></div>
<div>專案名稱： <%= @project.title %></div>
<div><%= @project.user.name%></div>
<div>NT$<%=@project.paid_orders_amounts.to_s(:delimited)%></div>
<div>已有<%= @project.paid_orders.count%>人贊助</div>
<div>還剩<%= @project.days_left%>天</div>


<%= link_to '專案', user_projects_path %>

<%# 這裡整個card要都能按連結 %> 
