<%= form_with(model: admin_dialogbox, local: true) do |form| %>
  <% if admin_dialogbox.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_dialogbox.errors.count, "error") %> prohibited this admin_dialogbox from being saved:</h2>

      <ul>
        <% admin_dialogbox.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
