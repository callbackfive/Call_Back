<%= form_with(model: admin_giveback, local: true) do |form| %>
  <% if admin_giveback.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_giveback.errors.count, "error") %> prohibited this admin_giveback from being saved:</h2>

      <ul>
        <% admin_giveback.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
